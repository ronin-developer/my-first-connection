<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My first conection</title>
    <link rel="stylesheet" href="bootstrap.css">
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-8 offset-2">
                <div class="row">
                    <h2 >Enter your full name and email</h2>
                    <form action="/create" method="post" class="form-control">
                        <input type="text" name="full_name" placeholder="full name" class="form-control m-2">
                        <input type="email" name="email" placeholder="email" class="form-control m-2">
                        <button class="btn btn-info m-2">Save</button>
                    </form>
                </div>
                <div class="row">
                    <% for( let index=0; index < data.length; index++ ) { %>
                        <%- include('partials/card.ejs', {user:data[index]}) %>
                            <% } %>
                </div>
            </div>
        </div>
    </div>

</body>

</html>